const SkillsPage = ({ skillsRef }) => {
    return (
        <section ref={skillsRef}>
            <div>
                <h1>My Skills</h1>
                <p>My knowledge</p>
                <div>

                </div>
            </div>
        </section>
    );
}

export default SkillsPage;